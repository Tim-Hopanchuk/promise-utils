{
  "name": "promise-utils",
  "version": "2.0.0",
  "description": "Lightweight, dependency-free TypeScript utilities for working with promises: delay execution, enforce timeouts, retry failing tasks, and run tasks sequentially with a pause between them.",
  "license": "ISC",
  "author": "Tim-Hopanchuk",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@vitest/ui": "^4.1.5",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
